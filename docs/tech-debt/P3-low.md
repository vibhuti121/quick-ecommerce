# P3 — Low

Smell / cleanup / cheap hardening. See [`README.md`](README.md) for the ID scheme, status lifecycle, and
routing.

| ID | Lens | Title | file:line | Failure mode | Suggested fix | Route-to | Status |
|---|---|---|---|---|---|---|---|
| TD-003 | security | BulkheadFilter guards two phantom routes; real protected paths get no overload bulkhead | `gateway/src/main/java/com/varsha/gateway/filter/BulkheadFilter.java:23-25` | `PROTECTED_ROUTES = {auth-service, room-service, community-service}`, but `room-service`/`community-service` are not gateway route ids (`application.yml:20-250` defines auth/catalog/cart/inventory/payment/order/videocall/signaling/storefront). So the per-route concurrency bulkhead only ever engages for `auth-service` and is dead config for the other two — leftover from a prior topology. Latent: the intended overload protection silently does nothing on those names, and the stale set invites a copy-paste bug if someone adds a `room`/`community` route assuming it's already protected. | Drop the dead ids; align `PROTECTED_ROUTES` with real expensive routes (e.g. add `catalog-service` search / `order-service` checkout if bulkhead protection is wanted there) and ensure each has a matching `resilience4j.bulkhead.instances` entry. | backend-orchestrator (→ security-agent) | open |
| TD-004 | security | Flat per-IP rate limit — no stricter cap on the auth/OTP/login brute-force surface at the edge | `gateway/src/main/java/com/varsha/gateway/filter/RateLimitFilter.java:50-78` + `application.yml:400-405` | A single global 100-req/60s bucket per IP covers every path equally; expensive/abuse-prone surfaces (`/auth/login`, `/auth/otp/request`, `/api/auth/google`, `/api/catalog/products/search`) get the same generous budget as cheap static GETs. Edge brute-force / OTP-spam / search-DoS pressure is only bounded by the shared 100/min. Partially mitigated — AuthFilter comments (`AuthFilter.java:29-32`) note auth-service enforces its own attempt-caps/anti-enumeration — so this is defense-in-depth hardening, not a live hole, hence P3. | Add a tighter per-path bucket (e.g. low burst on `/auth/login`,`/auth/otp/`,`/api/auth/google`) layered over the global limit; keep the existing flat bucket as the floor. Confirm auth-service's own caps remain the primary control. | backend-orchestrator (→ security-agent) | open |
