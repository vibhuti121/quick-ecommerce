<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.14</version>
    <relativePath/>
  </parent>

  <groupId>com.varsha</groupId>
  <artifactId>catalog-service</artifactId>
  <version>1.0.0</version>
  <name>catalog-service</name>

  <properties>
    <java.version>21</java.version>
    <!-- Boot 3.5.12's BOM still pins Tomcat 10.1.52, which carries CVE-2026-41293 (HTTP/2 header
         validation); fix is 10.1.55, a drop-in within the 10.1.x line. Override until a Boot patch
         ships 10.1.55+. This service bundles no spring-security. -->
    <tomcat.version>10.1.55</tomcat.version>
    <!-- Boot 3.5.14's BOM still pins pgjdbc 42.7.10, which carries CVE-2026-42198 (client-side
         DoS); fix is 42.7.11, a drop-in within the 42.7.x line. Override until a Boot patch ships
         42.7.11+. -->
    <postgresql.version>42.7.11</postgresql.version>
    <aws.sdk.version>2.25.70</aws.sdk.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- AWS SDK v2 BOM — aligns the s3 client + its transitive AWS modules on one version. -->
      <dependency>
        <groupId>software.amazon.awssdk</groupId>
        <artifactId>bom</artifactId>
        <version>${aws.sdk.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-cache</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <!-- Flyway 10+ (Boot 3.5) moved PostgreSQL support into a separate module; without it the app
         fails at startup with "Unsupported Database: PostgreSQL". Version managed by the Boot BOM. -->
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
    </dependency>
    <dependency>
      <groupId>net.logstash.logback</groupId>
      <artifactId>logstash-logback-encoder</artifactId>
      <version>7.4</version>
    </dependency>
    <!-- S3-compatible object storage (MinIO) for product images, Phase 2 Pillar 4. -->
    <dependency>
      <groupId>software.amazon.awssdk</groupId>
      <artifactId>s3</artifactId>
    </dependency>
    <!-- OpenSearch low-level Java client (Phase 2, search pillar). Chosen over spring-data-opensearch,
         which is version-coupled to specific Boot lines (the #1 build risk vs Boot 3.5.x); this client
         is version-decoupled. It transitively pulls Apache httpclient5/httpcore5 + jackson — both managed
         by the Boot BOM, so versions align with the rest of the service (verified by `compose build`). -->
    <dependency>
      <groupId>org.opensearch.client</groupId>
      <artifactId>opensearch-java</artifactId>
      <version>2.11.1</version>
    </dependency>
    <!-- opensearch-java declares the Apache httpclient5 transport as optional, so it must be brought
         in explicitly. Version is managed by the Boot 3.5 BOM (httpclient5.version) → aligned with
         the rest of the service; do NOT pin a version here. -->
    <dependency>
      <groupId>org.apache.httpcomponents.client5</groupId>
      <artifactId>httpclient5</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
